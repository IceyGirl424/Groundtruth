# Secrets — never commit
.env

# Python
__pycache__/
*.pyc
*.pyo

# Runtime logs
logs/

# Dashboard runtime feed (written by the coordinator)
fleet_state.json

# uAgents local runtime state (per-agent storage; machine-local)
*.agent
*_data.json
